# تشغيل "فلور لينك" على استضافة خاصة فيك (مجاني بالكامل، بدون بطاقة أبدًا)

هاد الدليل بيوصلك من صفر لتطبيق شغّال على رابط خاص فيك، بدون أي دفع، وبدون بطاقة بنكية، وبدون Claude نهائيًا.

## شو بده يصير بهالخطوات

- **Firebase (Firestore فقط)** — مجاني بدون بطاقة أبدًا: بديل قاعدة البيانات يلي كانت من Claude (الحسابات، الورديات، الزيارات...).
- **ImgBB** — مجاني بدون بطاقة أبدًا: بديل رفع الصور والتواقيع.
- **GitHub Pages** — مجاني بالكامل: استضافة الملفات، الرابط بيكون تبعك إنت مية بالمية.

⚠️ **ملاحظة مهمة**: جربنا الأول Firebase Storage لرفع الصور، بس Google صارت تطلب بطاقة بنكية إلها (حتى لو ما رح تدفع شي فعليًا). جربنا بعدها Cloudinary بس طلعت غير متوفرة ببعض البلدان. استقرينا على **ImgBB** يلي ما بيطلب بطاقة ولا قيود بلد معروفة.

الوقت المتوقع: ٢٠-٣٠ دقيقة أول مرة.

---

## الخطوة ١: إنشاء مشروع Firebase (لقاعدة البيانات فقط)

1. روح لـ **https://console.firebase.google.com** وسجّل دخول بحساب Google.
2. دوس **"Add project"**.
3. اعطيه اسم (مثلاً `floor-link`)، Continue، عطّل Google Analytics (مو ضروري)، **Create project**.

## الخطوة ٢: تفعيل Firestore (قاعدة البيانات)

1. من القائمة الجانبية: **Build → Firestore Database**.
2. **Create database**.
3. اختار **Start in production mode**.
4. اختار أقرب موقع سيرفر (مثلاً `eur3` أو `me-west1`).
5. **Enable**.

**لا تفتح Storage نهائيًا** — ما إلها داعي هلق، وهي يلي بتطلب البطاقة.

## الخطوة ٣: أخذ إعدادات Firebase (الـ Config)

1. أيقونة الترس ⚙️ فوق يسار → **Project settings**.
2. تحت "Your apps"، أيقونة **</>** (Web app).
3. اسم (مثلاً `floor-link-web`)، **بدون** تفعيل Hosting، **Register app**.
4. رح يطلعلك كود فيه شي متل هيك:

```js
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "floor-link-xxxxx.firebaseapp.com",
  projectId: "floor-link-xxxxx",
  storageBucket: "floor-link-xxxxx.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef123456"
};
```

5. افتح ملف **`index.html`** يلي بعتلك ياه، دوّر قريب من البداية على:

```js
var YOUR_FIREBASE_CONFIG = {
  apiKey: "PASTE_YOUR_API_KEY",
  ...
};
```

6. بدّل كل قيمة `"PASTE_..."` بالقيمة المطابقة يلي نسختها. خلي علامات الاقتباس " ".
7. احفظ.

## الخطوة ٤: تفعيل صلاحيات Firestore

1. **Firestore Database → Rules**.
2. امسح كل شي، ألصق محتوى ملف **`firestore.rules`** يلي بعتلك ياه.
3. **Publish**.

⚠️ هالإعدادات بتخلي أي حدا معه رابط تطبيقك يوصل لقاعدة البيانات — نفس المستوى يلي كان بـ Claude تقريبًا. مناسب لتطبيق داخلي، بس مش حماية بمستوى بنك.

## الخطوة ٥: أخذ مفتاح ImgBB (لرفع الصور، بدون بطاقة)

1. روح لـ **https://api.imgbb.com/** واعمل حساب مجاني (إيميل وباسوورد بس، ما رح يطلب بطاقة).
2. بعد التسجيل، الموقع رح يطلعلك مباشرة **"Your client API key"** — انسخه.
3. رجع لملف `index.html`، دوّر على:

```js
var YOUR_IMGBB_CONFIG = {
  apiKey: "PASTE_YOUR_IMGBB_API_KEY"
};
```

4. حط المفتاح يلي نسخته بدل `PASTE_YOUR_IMGBB_API_KEY`، بين علامات الاقتباس.
5. احفظ.

خطة ImgBB المجانية بتسمح بصور لحد ٣٢ ميجا لكل صورة، ومحفوظة بشكل دائم — كافية جدًا لصور ورديات محل.

## الخطوة ٦: رفع الملفات عبى GitHub Pages

1. اعمل حساب مجاني على **https://github.com** إذا ما عندك.
2. **"+"** فوق يمين → **New repository**.
3. اسم الريبو (مثلاً `floor-link`)، خليه **Public**، **Create repository**.
4. بصفحة الريبو، **"uploading an existing file"**.
5. اسحب الملفات الثلاثة:
   - `index.html`
   - `manifest.json`
   - `sw.js`
6. **Commit changes**.
7. **Settings** → **Pages** (بالقائمة الجانبية).
8. تحت "Branch"، اختار **main** والمجلد **/ (root)** → **Save**.
9. استنى دقيقة-دقيقتين، رح يطلعلك رابط:
   `https://<اسم-حسابك>.github.io/floor-link/`

هاد رابط تطبيقك النهائي — دائم، مجاني، خاص فيك بالكامل، بدون أي بطاقة بنكية بأي مكان بالعملية كلها.

## الخطوة ٧: أول استخدام

1. افتح الرابط من تلفونك.
2. أول شخص بيسجل حساب جديد وياخد دور **"مدير/أدمن"** (ما في مدير مسجل لسا — التطبيق بيسمحلك هالخيار مرة وحدة بس، أول تسجيل).
3. من هلق فصاعدًا، أي حدا تاني بيسجل بيكون سوبرفايزر أو مارشندايزر بس، وإنت كمدير بترفعه لمدير لاحقًا من قسم "الحسابات".
4. "Add to Home Screen" هلق رح تعطي أيقونتكم الحقيقية، وبتشتغل أوفلاين فعليًا، والكاميرا للباركود بتشتغل صح.

---

## إذا صار خطأ

- **الصفحة بيضا أو ما بتفتح**: افتح Developer Console بالمتصفح (F12 عالكمبيوتر) وشوف الأخطاء الحمراء — الأغلب Firebase Config مش مظبوط.
- **تسجيل الدخول ما بيشتغل / "permission denied"**: تأكد من خطوة ٤ (Publish على firestore.rules).
- **الصور ما عم ترفع**: تأكد إن المفتاح بـ `index.html` (خطوة ٥) منسوخ صح وبدون مسافات زيادة، وإن الحساب على ImgBB مفعّل (تأكيد الإيميل إذا طلب منك).

أي مشكلة تانية، ابعتلي لقطة شاشة لرسالة الخطأ وبساعدك.
